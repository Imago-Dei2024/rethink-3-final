import withBundleAnalyzer from '@next/bundle-analyzer';

const bundleAnalyzer = withBundleAnalyzer({
  enabled: process.env.ANALYZE === 'true',
});

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable image optimization
  images: {
    formats: ['image/avif', 'image/webp'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
    minimumCacheTTL: 60,
  },
  // Enable compression
  compress: true,
  // Increase stability
  experimental: {
    // Use React's new optimized concurrent features
    serverActions: {
      bodySizeLimit: '2mb', // Example value, adjust as needed
    },
    // Enable scroll restoration for better user experience
    scrollRestoration: true,
  },
  // Improve performance by eliminating console in production
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production',
  },
  // Add content security policy for increased stability
  headers: async () => [
    {
      source: '/(.*)',
      headers: [
        {
          key: 'X-Content-Type-Options',
          value: 'nosniff',
        },
        {
          key: 'X-Frame-Options',
          value: 'DENY',
        },
        {
          key: 'X-XSS-Protection',
          value: '1; mode=block',
        },
      ],
    },
  ],
};

export default bundleAnalyzer(nextConfig);